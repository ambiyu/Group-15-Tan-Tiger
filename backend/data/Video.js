class Video {
    constructor(videoID, title, channel, thumbnail, duration) {
        this.videoID = videoID;
        this.title = title;
        this.channel = channel;
        this.thumbnail = thumbnail;
        this.duration = duration;
    }
}

module.exports = Video;
