// Represents the information that a user has on the server.
// Can be expanded later, initial implementation will just involve a user name

class User {
    constructor(userName) {
        this.userName = userName;
    }
}

module.exports = User;
